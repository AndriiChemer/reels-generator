export default function CharactersPage() {
  return (
    <section className="rounded-lg border border-[var(--border)] bg-[var(--panel)] p-6">
      <p className="text-sm font-medium text-[var(--accent-strong)]">Task 04</p>
      <h1 className="mt-2 text-3xl font-semibold tracking-normal">
        Персонажі
      </h1>
      <p className="mt-3 max-w-2xl text-base leading-7 text-[var(--muted)]">
        Тут буде локальний список recurring characters. У поточному task
        створена тільки сторінка-заглушка для навігації.
      </p>
    </section>
  );
}

